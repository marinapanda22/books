<p id="notice"><%= notice %></p>

<h3><%= @category.title %></h3>
<% @category.books.each do |book|%>
    <%= book.title %>
    <div class="img"><%= link_to image_tag(book.image.large), book_path(book) %></div>
    <%= book.user.name %>
<% end %>

</p>

